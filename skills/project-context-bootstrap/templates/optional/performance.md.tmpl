<!-- TEMPLATE_QUESTIONS
1. 项目有哪些关键性能指标和目标？（如：P99 < 200ms / QPS > 1000 / 内存 < 2GB）
2. 已知的性能瓶颈在哪里？（数据库慢查询、外部 API 调用、CPU 密集计算等）
3. 哪些代码区域是性能敏感的，改动需要特别谨慎？
4. 团队有哪些"性能反模式"是禁止的？（如 N+1 查询、循环里调远程接口）
5. 怎么压测？怎么 profile？
-->

# 性能（Performance）

> 项目的性能要求和优化约定。改性能敏感区域前必读。

## 关键性能指标

| 指标 | 目标 | 当前 | 监控位置 |
|---|---|---|---|
| {{指标1}} | {{目标1}} | {{当前1}} | {{监控1}} |
| {{指标2}} | {{目标2}} | {{当前2}} | {{监控2}} |

例如：
- P99 响应时间：< 200ms（API 网关 Grafana 面板）
- 单实例 QPS：> 1000
- 内存峰值：< 2GB

## 已知瓶颈

### 瓶颈 1：{{瓶颈1标题}}

**位置**：{{文件/模块1}}

**原因**：{{原因1}}

**缓解措施**：{{措施1}}

---

### 瓶颈 2：{{瓶颈2标题}}

（同上结构）

## 性能敏感区域

以下代码区域是 hot path，**改动前必须**考虑性能影响：

| 区域 | 原因 | 改动注意事项 |
|---|---|---|
| {{区域1}} | {{原因1}} | {{注意1}} |
| {{区域2}} | {{原因2}} | {{注意2}} |

## 禁止的性能反模式

{{反模式列表}}

例如：
- ❌ **N+1 查询**：循环里调数据库，必须 batch
- ❌ **循环里调外部 API**：必须并发或缓存
- ❌ **同步阻塞 IO 在请求线程**：必须用异步或独立线程池
- ❌ **大对象进 Redis**：> 10KB 应分片或换存储

## 压测与 Profile

### 压测

```bash
{{压测命令}}
```

### Profile

```bash
{{profile 命令}}
```

## AI 写代码时的注意

- ⚠️ 改性能敏感区域前先看本文档
- ⚠️ 引入新的外部调用前考虑是否需要缓存
- ❌ 不要在不知道数据量的情况下写没有 LIMIT 的查询

---

*性能要求变化或发现新瓶颈时，更新本文件。*
