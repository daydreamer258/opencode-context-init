<!-- TEMPLATE_QUESTIONS
1. 这个项目部署到哪些环境？（如：dev / staging / prod，或单一环境）
2. 部署用什么工具/流程？（如：CI/CD pipeline / 手动脚本 / k8s / docker / ssh + supervisor）
3. 部署前必须做哪些检查？（迁移、配置、依赖等）
4. 部署完成后如何验证？
5. 出问题如何回滚？
6. AI 在部署相关代码上有哪些边界？（通常应只读不写 deploy 配置）
-->

# 部署（Deployment）

> 项目的部署流程和 AI 在其中的边界。**通常 AI 不应直接执行部署。**

## 部署目标环境

{{部署环境列表}}

例如：
- `dev` — 开发环境，自动部署 main 分支
- `staging` — 预发环境，手动触发
- `prod` — 生产环境，需要审批

## 部署流程

### 部署前检查

部署前**必须**确认：

- [ ] {{检查1}}
- [ ] {{检查2}}
- [ ] {{检查3}}

例如：
- [ ] 所有测试通过
- [ ] 数据库 migration 在预发环境已验证
- [ ] 配置变更已在 dev/staging 验证过

### 部署命令

```bash
{{部署命令}}
```

### 部署后验证

```bash
{{部署后验证命令}}
```

或人工检查：
- {{人工检查1}}
- {{人工检查2}}

## 回滚流程

{{回滚步骤}}

例如：
- 自动回滚条件：错误率 > X%
- 手动回滚：`kubectl rollout undo deployment/xxx`
- 数据库回滚：需要专门 DBA 操作，不要自助

## AI 在部署上的边界

- ❌ AI **不要直接执行**部署命令——即使能跑，应让用户最终触发
- ❌ AI **不要修改**部署配置（CI/CD yaml、Dockerfile、k8s manifest）除非用户明确要求
- ⚠️ AI 可以**审阅**部署配置并提出建议
- ✅ AI 可以协助生成部署后的验证脚本

---

*部署流程变化时，更新本文件。*
