<!-- TEMPLATE_QUESTIONS
问 Agent 在 Phase 2 C 组里依次问用户的问题：
1. 这个项目能让 AI 自己跑命令完成验证吗？（yes / no / partial）
2. 如果能：验证一个改动的标准命令是什么？预期输出/通过判据是什么？
3. 验证失败时，应该按什么顺序排查？
4. 如果不能或只能部分自验证：AI 改完之后应该给用户哪些信息辅助用户验证？
-->

# 验证（Verification）

> Agent 改完代码后，按本文档判断"是否做完了"。

## AI 能否自验证

**结论**：{{能否自验证}}（yes / no / partial）

**原因**：{{原因说明}}

例如：
- yes — 项目是 CLI 工具/纯函数库，跑命令能直接看输出
- no — 项目依赖人工操作或外部环境（如 Spring Boot Web 项目需要启动服务 + 浏览器测试）
- partial — 单元测试能自动跑，但集成验证依赖人

## 标准验证流程

### 如果可以自验证

```bash
{{自验证命令}}
```

**通过判据**：
- {{判据1}}
- {{判据2}}

**典型输出示例**：
```
{{预期输出示例}}
```

### 如果不能完全自验证

AI 改完之后**必须**给用户列出：

1. **本次改动的范围**：哪些文件、改了什么
2. **建议的验证步骤**：{{用户验证步骤}}
3. **应该看到的现象**：{{预期现象}}
4. **失败可能的原因**：{{失败可能原因}}

## 失败排查顺序

验证失败时，按以下顺序检查：

1. {{排查1}}
2. {{排查2}}
3. {{排查3}}

## AI 行为约束

- ❌ 不要**只**说"改完了"——必须按本文档跑或列验证步骤
- ❌ 自验证失败时不要假装通过——如实告知用户
- ❌ 不要跳过验证步骤直接告诉用户"应该可以了"

---

*验证方式变化时，更新本文件。*
