<!-- TEMPLATE_QUESTIONS
1. 对外 API 是什么风格？（REST / GraphQL / gRPC / RPC / 混合）
2. URL/方法/资源命名有什么约定？
3. 请求/响应的标准格式？（如：统一包一层 envelope、错误码体系）
4. 版本管理怎么做？（URL 版本 / Header / 不版本）
5. 鉴权方式？
-->

# API 设计约定

> 对外 API 的设计原则。新增/修改接口时必须遵守。

## API 风格

{{API 风格说明}}

例如：
- 主要：REST + JSON
- 内部服务间：gRPC
- 实时通信：WebSocket

## URL / 资源命名

{{URL 约定}}

例如：
- 复数名词：`/users` 不是 `/user`
- 嵌套层级最多 2 级：`/users/{id}/orders` 可以，`/users/{id}/orders/{oid}/items` 不行（改为 `/orders/{oid}/items`）
- 动作不放 URL，用 HTTP method 表达；特殊动作用 `/resources/{id}/<action>` 子路径

## 请求/响应格式

### 标准响应

```json
{{标准响应示例}}
```

例如：
```json
{
  "code": 0,
  "message": "ok",
  "data": { ... }
}
```

### 错误响应

```json
{{错误响应示例}}
```

### 错误码体系

{{错误码说明}}

例如：
- 0 = 成功
- 1xxxx = 业务错误
- 4xxxx = 客户端错误（参数等）
- 5xxxx = 服务端错误
- 错误码必须在 `ErrorCode` 枚举里定义，不要散落字符串

## 版本管理

{{版本策略}}

例如：
- URL 版本：`/v1/users`
- 大版本不向后兼容时新开 `/v2/`
- 同版本内只允许向后兼容的变更（加字段可以、删字段不行）

## 鉴权

{{鉴权方式}}

例如：
- 用户接口：JWT in `Authorization: Bearer <token>`
- 服务间：mTLS

## AI 修改 API 时的约束

- ❌ 不要修改已发布接口的响应字段类型/语义
- ❌ 不要删字段，要 deprecate
- ⚠️ 新增字段时确认是否影响客户端（如 mobile 强解析）
- ✅ 内部接口改动相对自由

---

*API 约定变化时，更新本文件。*
