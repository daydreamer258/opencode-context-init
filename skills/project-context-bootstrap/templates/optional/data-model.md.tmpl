<!-- TEMPLATE_QUESTIONS
1. 项目数据存在哪里？（关系数据库 / NoSQL / 文件 / 多种）
2. 核心实体有哪些？最重要的 3-5 个表/集合是什么？它们的关键关系？
3. 有哪些必须遵守的索引/约束/外键规则？
4. migration / schema 变更走什么流程？
5. AI 在数据模型上的边界？（如"不要直接改 migration 已上线版本"）
-->

# 数据模型（Data Model）

> 项目的数据存储结构。Agent 在改涉及数据的代码前应先看这里。

## 数据存储概览

{{存储类型说明}}

例如：
- 主库：PostgreSQL 14，承载所有业务数据
- 缓存：Redis 7，短时缓存 + 分布式锁
- 文件：S3，存用户上传

## 核心实体

### {{实体1}}

**用途**：{{实体1用途}}

**关键字段**：
| 字段 | 类型 | 含义 | 约束 |
|---|---|---|---|
| {{字段1}} | {{类型1}} | {{含义1}} | {{约束1}} |
| {{字段2}} | {{类型2}} | {{含义2}} | {{约束2}} |

**与其他实体的关系**：{{关系说明}}

---

### {{实体2}}

（同上结构）

---

## 关键索引与约束

{{关键索引列表}}

例如：
- `users.email` UNIQUE — 注册时去重依赖
- `orders.created_at` 倒序索引 — 列表分页 hot path

## Schema 变更流程

{{migration 流程}}

例如：
- 新增 migration：`db/migration/V<编号>__<描述>.sql`
- 编号必须递增
- **已上线的 V 文件绝对不能改**，只能新增（见 boundaries.md）
- 大表 migration 必须 review，可能需要在线 DDL 工具

## AI 边界

- ❌ 不要直接改已上线的 migration
- ❌ 不要在不知道数据量的情况下加 `DROP COLUMN` 或 `ALTER COLUMN TYPE`
- ⚠️ 涉及核心实体表的改动要先告知用户
- ✅ 新增表 / 新增字段（nullable）相对安全

---

*Schema 演化时，更新本文件。*
