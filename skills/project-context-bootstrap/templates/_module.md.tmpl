---
触发条件: {{触发条件}}（例：改 {{模块名}} 相关代码 / 看到 {{特征关键词}} 报错 / 涉及 {{相关业务}}）
最后更新: {{今日日期}}
关联代码: {{关联代码路径}}（例：src/{{模块名}}/, packages/{{模块名}}/）
---

# {{模块名}}

> {{模块的一句话职责}}

每条记忆是一个独立的"原子条目"，编号 `M-NNN`（模块条目，区分全局条目 `G-NNN`）。

---

## 模块设计与决策

<!-- 这块模块"为什么是这样设计"——是关于不变性的，比代码本身更稳定。
     比如：为什么用这个状态机 / 为什么拆成两个 service / 为什么选这个第三方库。 -->

### M-001: {{决策1标题}}  `[decision]`

- **关联代码**: {{相关路径1}}
- **新增**: {{新增日期1}}
- **最后验证**: {{验证日期1}}
- **标签**: {{标签1}}

**事实/决策**: {{决策内容}}

**为什么**: {{原因 / 背景 / 当时的权衡}}

**反例**: {{看似合理但行不通的方案}}（可选）

---

## 约束与边界

<!-- 这个模块特有的"不能这样做"——比代码注释更显式。
     例：账户余额必须用 Decimal 不能用 float；状态转换必须经状态机不能直接 setStatus。 -->

### M-{{编号}}: {{约束标题}}  `[constraint]`

（同上结构）

---

## 陷阱与历史坑

<!-- 痛苦换来的知识——这块踩过的 bug、看起来能改但改了出问题的地方。
     每条写：现象 → 根因 → 现在的正确做法。 -->

### M-{{编号}}: {{陷阱标题}}  `[gotcha]`

- **关联代码**: {{相关路径}}
- **新增**: {{日期}}
- **最后验证**: {{日期}}
- **标签**: {{标签}}

**事实**: {{踩坑现象描述}}

**为什么**: {{根因}}

**反例**: {{当时尝试过但失败的做法}}

---

## 本模块 SOP

<!-- 这块有没有"做某件事的标准步骤"？没有就删本节。
     例：新增一个状态时的 5 步 / 加新事件类型时怎么注册。 -->

### M-{{编号}}: {{SOP 标题}}  `[sop]`

**适用场景**: {{何时用}}

**步骤**:
1. {{步骤1}}
2. {{步骤2}}

**注意**: {{注意事项}}

---

*新增条目用 `/remember`，删除用 `/forget M-NNN`，更新用 `/update-memory M-NNN`。*
