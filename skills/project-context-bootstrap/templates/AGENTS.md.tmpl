# {{项目名}}

> {{一句话描述：解决什么问题、给谁用}}

本文件是项目知识库的**唯一入口**。读完它你就有完整地图，知道在什么任务下展开哪个详细文档。

---

## § 1 项目元信息

| 项 | 值 |
|---|---|
| 主语言 | {{主语言}} |
| 主框架 | {{主框架}} |
| 包管理/构建 | {{包管理工具}} |
| 项目类型 | {{项目类型}}（如：CLI / Web 服务 / 库 / Monorepo / ...） |
| 仓库布局 | {{布局描述}}（如：扁平 / 按模块分包 / monorepo with workspaces） |

### Agent 工作模式偏好

| 能力 | 是否允许 | 备注 |
|---|---|---|
| 自动运行测试 | {{是否允许测试}} | {{测试备注}} |
| 自动启动服务 | {{是否允许启服}} | {{启服备注}}（如 "禁止：Spring 启动慢且占端口，由人工启" / "可以：CLI 项目即跑即停"） |
| 自动跑 build | {{是否允许构建}} | {{构建备注}} |
| 自动执行 migration | **禁止** | migration 一律由人工执行 |
| 自动 git commit / push | **禁止** | 由用户决定 |
| 自动写盘到 docs/agent/ | **禁止** | 必须经 `/remember` 等命令显式触发 |

---

## § 2 能力清单与禁区

### ✅ 可以做的

{{可做清单}}

例如：
- 读任意源码文件并解释
- 修改 `src/` 下的代码（不涉及 § 3 钩子要求的前置阅读时）
- 在 `test/` 下添加/修改测试
- 运行 `{{允许的命令1}}`、`{{允许的命令2}}` 等只读/可逆命令

### ❌ 不可以做的

{{禁区清单}}

例如：
- ❌ 修改 `{{禁区目录1}}`（{{原因1}}）
- ❌ 修改任何 `db/migration/V*.sql` 已上线版本——只能新增高编号 migration
- ❌ 修改 CI/CD 配置（`.github/`、`Jenkinsfile` 等）除非用户明确要求
- ❌ 运行任何会**触达外部系统**的命令（部署、对第三方 API 发请求等）
- ❌ 删除文件，除非用户明确说"删 X"

### ⚠️ 需要先征求用户同意的

{{需确认清单}}

例如：
- ⚠️ 改动任何 `{{敏感目录}}` 下的文件
- ⚠️ 引入新的依赖（package.json / pom.xml / Cargo.toml）
- ⚠️ 重命名公共 API 或导出符号

---

## § 3 工作流钩子（Intent → 必读文档）

识别到以下意图时，**先读对应文档再动手**：

| 你打算做的事 | 必读 | 为什么 |
|---|---|---|
| {{钩子1意图}} | {{钩子1文档}} | {{钩子1原因}} |
| {{钩子2意图}} | {{钩子2文档}} | {{钩子2原因}} |

例如：
| 修改数据库 schema 或加 migration | `docs/agent/_global.md` § 数据 | 历史踩过多次迁移问题 |
| 改任何 OAuth / 认证流程 | `docs/agent/auth.md` | 这块有并发竞争陷阱 |
| 新增对外 API 接口 | `docs/agent/_global.md` § API 约定 | 错误码、版本约定不能违反 |
| 改 `legacy/` 任何文件 | 找用户确认 | 该目录原则上只读 |

---

## § 4 文档索引（何时读，而非写了什么）

按"何时需要"列出。**不要尝试一次性读完所有文档**——按当前任务匹配触发条件再展开。

| 文档 | 何时读 | 主题标签 |
|---|---|---|
| [docs/agent/_global.md](docs/agent/_global.md) | 跨模块的全局约定、数据约定、API 约定、历史重大决策 | global, conventions, history |
| {{模块文档1}} | {{模块1触发条件}} | {{模块1标签}} |
| {{模块文档2}} | {{模块2触发条件}} | {{模块2标签}} |

例如（实际索引由 `/init-project` 根据发现的模块生成）：
| docs/agent/auth.md | 改认证/授权相关代码、看到 token/session 相关报错 | auth, oauth, session |
| docs/agent/billing.md | 改计费、订阅、支付相关代码 | billing, payment, subscription |

---

## § 5 给 Agent 的元规则

1. **不可推导性优先**：你能从代码直接读出来的事实，不要追问用户、也不要建议归档
2. **任务前读钩子**：动手前对照 § 3 工作流钩子，命中就先读对应文档
3. **不擅自写记忆**：发现可能值得记的事时，**只建议**用户跑 `/remember`，不要直接改 `docs/agent/`
4. **任务结束自查**：完成任务后回顾——本次有没有"早知道就不会犯的错"？有，主动建议 `/remember`
5. **遇到不确定，问用户**：宁可问一次，也不要按猜测改代码

{{项目特有的元规则}}

---

*本文件由 `/init-project` 生成。增量更新用 `/remember`、`/forget`、`/update-memory`；定期检查用 `/audit-memory`。*
