---
触发条件: 涉及跨模块的全局约定 / 数据约定 / API 约定 / 重大历史决策时
最后更新: {{今日日期}}
关联代码: 整个仓库
---

# 全局约定与历史

本文件存放**跨模块**或**没有模块归属**的事实和决策。模块专属内容请去 `docs/agent/<模块名>.md`。

每条记忆是一个独立的、可单独删除/更新的"原子条目"，编号 `G-NNN`（global 前缀，区分模块条目 `M-NNN`）。

---

## 全局约定（跨模块的代码层面规则）

<!-- 这些条目说明：在多个模块共享的、模型不容易从代码推导的规则。
     例：错误处理风格、日志格式、依赖注入约定、序列化策略等。
     注意：不要写代码里能直接看到的东西（如"用 Java"、"用 Maven"）。 -->

### G-001: {{条目1标题}}  `[constraint | convention]`

- **关联代码**: {{相关路径1}}
- **新增**: {{新增日期1}}
- **最后验证**: {{验证日期1}}
- **标签**: {{标签1}}

**事实**: {{事实1}}

**为什么**: {{原因1}}

**反例**: {{反例1}}（可选）

---

## 数据约定

<!-- 数据库 schema、消息格式、序列化、migration 流程等。
     重点写"为什么这样"和"踩过的坑"，不写当前 schema 长什么样（schema 看代码即可）。 -->

### G-{{编号}}: {{数据条目标题}}  `[constraint | gotcha | sop]`

（同上结构）

---

## 对外 API 约定

<!-- 如果项目对外有 API：URL/方法/错误码/版本管理 等强约定。
     如果是纯库或纯 CLI 项目，本节可删。 -->

### G-{{编号}}: {{API 条目标题}}  `[constraint | convention]`

（同上结构）

---

## 历史重大决策与事故

<!-- 这里记录"痛苦换来的知识"——调试过的 bug、生产事故、看似合理但行不通的方案。
     最高价值的记忆。一定要写清楚"踩坑现象 → 根因 → 现在为什么这样做"。 -->

### G-{{编号}}: {{历史条目标题}}  `[gotcha | decision]`

- **关联代码**: {{相关路径}}
- **新增**: {{日期}}
- **最后验证**: {{日期}}
- **标签**: {{标签}}

**事实/决策**: {{发生了什么 + 现在的做法}}

**为什么**: {{根因 + 为什么选这个方案}}

**反例**: {{看似合理但行不通的方案}}

---

## SOP（操作流程）

<!-- 不是约定，而是"做某件事的标准步骤"。
     例：发版流程、回滚步骤、紧急修复流程。
     与 AGENTS.md § 3 工作流钩子配合：钩子说"读哪段 SOP"，这里写具体步骤。 -->

### G-{{编号}}: {{SOP 条目标题}}  `[sop]`

- **关联代码/工具**: {{相关路径或工具}}
- **新增**: {{日期}}
- **最后验证**: {{日期}}
- **标签**: {{标签}}

**适用场景**: {{什么时候用这个 SOP}}

**步骤**:
1. {{步骤1}}
2. {{步骤2}}
3. {{步骤3}}

**注意**: {{注意事项}}

---

*新增条目用 `/remember`，删除用 `/forget G-NNN`，更新用 `/update-memory G-NNN`，定期检查用 `/audit-memory`。*
