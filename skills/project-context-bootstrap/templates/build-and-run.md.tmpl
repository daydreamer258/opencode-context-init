# 构建与运行

## 环境依赖

| 依赖 | 版本要求 | 安装方式 |
|---|---|---|
| {{依赖1}} | {{版本1}} | {{安装1}} |
| {{依赖2}} | {{版本2}} | {{安装2}} |

## 标准流程

### 首次拉取后

```bash
{{首次准备命令}}
```

### 日常构建

```bash
{{构建命令}}
```

### 启动运行

```bash
{{运行命令}}
```

### 运行测试

```bash
{{测试命令}}
```

## 代码生成流程（重要）

**模型最容易忽略的部分。** 修改这些来源文件后，必须按下述顺序执行命令。

{{代码生成说明}}

例如：
- **YAML → 接口代码**
  - 来源：`src/main/resources/openapi/*.yaml`
  - 触发：`mvn compile`（自动）或 `mvn generate-sources`（手动）
  - 输出：`target/generated-sources/openapi/`
  - AI 规则：**只改 YAML，不要直接改生成代码**

- **Proto → gRPC stub**
  - 来源：`proto/*.proto`
  - 触发：`make protoc`
  - 输出：`gen/`
  - AI 规则：**改完 proto 必须重跑 make protoc**

## 常用快捷命令

| 场景 | 命令 |
|---|---|
| {{场景1}} | `{{命令1}}` |
| {{场景2}} | `{{命令2}}` |

## 排错

如果构建/运行失败，**优先**检查以下顺序：

1. {{排错步骤1}}
2. {{排错步骤2}}
3. {{排错步骤3}}

---

*配置变更（端口、数据库连接、密钥）应在哪里改？参见 `boundaries.md` 中的敏感文件清单。*
