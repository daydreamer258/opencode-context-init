# AI 边界

> Agent 在改代码前**必须**先查这个文件。本文件定义"哪些地方不能动"。

## 绝对禁区（不要改）

以下文件/目录，AI **永远不要**直接修改：

| 路径 | 原因 | 替代做法 |
|---|---|---|
| {{禁区1}} | {{原因1}} | {{替代1}} |
| {{禁区2}} | {{原因2}} | {{替代2}} |

常见类型：

- **自动生成的代码**：如 `target/generated-sources/`、`build/generated/`、`gen/` —— 改了下次构建会被覆盖；应改来源文件（yaml、proto 等）
- **第三方依赖代码**：如 `vendor/`、`node_modules/`、`third_party/` —— 不属于本项目
- **已上线的数据库 migration**：如 `db/migration/V*.sql` 中已部署的版本 —— 改了会导致环境间不一致；应新增高编号 migration

## 敏感文件（涉及才能改，且必须告知用户）

包含密钥、生产配置、合规相关的文件。**修改前必须明确告诉用户你要改什么**：

| 路径 | 内容 | 注意事项 |
|---|---|---|
| {{敏感1}} | {{内容1}} | {{注意1}} |
| {{敏感2}} | {{内容2}} | {{注意2}} |

例如：
- `src/main/resources/application-prod.yml` — 生产环境配置
- `.env.production` — 生产密钥（如果在仓库里）
- `infra/terraform/*.tf` — 基础设施定义

## 只读区（只能读，不能写）

可以阅读以理解上下文，但**不要修改**：

{{只读区列表}}

例如：
- `legacy/` — 老代码，正在被新模块逐步替代，不接受新功能
- `experimental/` — 实验性代码，未来可能整体删除

## 改前必须征求确认的区域

不是禁区，但变更影响面大、容易引入回归：

{{需确认区域}}

例如：
- 公共 API 接口定义（`*ApiController.java`）
- 数据库实体类（影响 migration）
- 全局配置类（`@Configuration` 类）
- 核心算法模块（如风控规则、计费逻辑）

**Agent 处理流程**：发现要改这些区域时，先说明改动的范围和影响，等用户确认。

## 允许自由修改的区域

为了平衡——明确列出"放心改"的部分，让 Agent 不必反复确认：

{{自由区列表}}

例如：
- `src/test/` 下的测试代码
- `docs/` 下的文档
- 内部工具类、私有方法

---

*边界会随项目演化变化。发现新的禁区或解禁某个区域时，请更新本文件。*
