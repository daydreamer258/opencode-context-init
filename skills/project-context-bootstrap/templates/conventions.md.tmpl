# 约定与风格

> 这些约定是项目专属的——可能与"业界最佳实践"不同。**优先遵守这里的约定**，不要按通用经验想当然。

## 包/目录结构

{{包结构说明}}

例如：
```
src/main/java/com/example/
├── controller/    # 仅 HTTP 入口，不写业务逻辑
├── service/       # 业务逻辑必须放这里
├── repository/    # 仅数据访问
└── model/         # 实体类
```

**关键规则：**
- {{结构规则1}}
- {{结构规则2}}

## 命名约定

| 类型 | 规则 | 示例 |
|---|---|---|
| {{类型1}} | {{规则1}} | {{示例1}} |
| {{类型2}} | {{规则2}} | {{示例2}} |

## 依赖注入 / 模块组织

{{注入与模块约定}}

例如：
- 必须用构造器注入，不允许字段注入
- 单例由 `XxxFactory` 统一创建，不要直接 `new`

## 错误处理

{{错误处理约定}}

例如：
- 所有业务异常继承 `BusinessException`
- Controller 层捕获并转 HTTP 状态码，业务层只抛不接
- 不允许吞掉异常（`catch (Exception e) {}` 一律禁止）

## 日志风格

{{日志约定}}

例如：
- 用 `slf4j` 不要用 `System.out`
- 日志级别约定：DEBUG=开发排查，INFO=关键节点，WARN=可恢复异常，ERROR=必须告警

## 测试约定

{{测试约定}}

例如：
- 单元测试文件命名 `XxxTest.java`，放在 `src/test/java/` 对应包下
- 集成测试用 `@SpringBootTest`，文件命名 `XxxIT.java`
- 必须用真实数据库，不要 mock Repository

## AI 写新代码时最容易选错的"风格"

{{AI 易犯错误}}

例如：
- AI 容易直接 `new Service()`，但本项目要求从 Spring 容器获取
- AI 容易用 `Optional.get()`，本项目要求始终 `orElseThrow`
- AI 容易把 Controller 写得很厚，本项目要求 Controller 不超过 20 行

---

*发现未被记录的约定？请提议追加到此文件。*
